# Kubernetes deployment files for default drupal/mysql deployment without customization
This directory contains the manifests to create persistent volumes claim, pod deployments, and service definitions.
